<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<meta name="theme-color" content="#07060d">
<title>pelis ok</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Monoton&family=Audiowide&family=Space+Grotesk:wght@400;500;600;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
<script src="https://unpkg.com/peerjs@1.5.4/dist/peerjs.min.js"></script>
<style>
:root{
  --bg:#07060d; --bg2:#0c0a18; --panel:#0e0b1c; --panel2:#14102a; --line:#241d44;
  --ink:#eaf2ff; --dim:#8b86b8; --dim2:#5d5888;
  --cyan:#19f0ff; --mag:#ff2bd6; --lime:#b6ff3a; --vio:#8b5cff; --yel:#ffe14d; --red:#ff4d6d;
  --glow-c:0 0 18px rgba(25,240,255,.55); --glow-m:0 0 18px rgba(255,43,214,.5);
  --r:14px; --vh:1vh;
}
*{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
html,body{height:100%}
body{
  background:var(--bg); color:var(--ink);
  font-family:"Space Grotesk",system-ui,sans-serif;
  height:100%; overflow:hidden; position:relative;
}
body::before{content:"";position:fixed;inset:0;z-index:-2;pointer-events:none;
  background:
    radial-gradient(900px 520px at 50% -10%, rgba(255,43,214,.16), transparent 60%),
    radial-gradient(800px 600px at 12% 110%, rgba(25,240,255,.12), transparent 60%),
    radial-gradient(700px 500px at 92% 96%, rgba(139,92,255,.12), transparent 60%),
    var(--bg);
}
body::after{content:"";position:fixed;left:0;right:0;bottom:0;height:46vh;z-index:-1;pointer-events:none;
  background-image:
    linear-gradient(rgba(25,240,255,.16) 1px,transparent 1px),
    linear-gradient(90deg,rgba(255,43,214,.12) 1px,transparent 1px);
  background-size:44px 44px,44px 44px;
  transform:perspective(420px) rotateX(62deg); transform-origin:bottom;
  -webkit-mask-image:linear-gradient(transparent,#000 70%); mask-image:linear-gradient(transparent,#000 70%);
  animation:grid 7s linear infinite;
}
@keyframes grid{to{background-position:0 44px,44px 0}}
.scanlines{position:fixed;inset:0;z-index:4000;pointer-events:none;opacity:.5;mix-blend-mode:overlay;
  background:repeating-linear-gradient(0deg,rgba(0,0,0,.22) 0 1px,transparent 1px 3px)}
::selection{background:var(--mag);color:#1a0014}
button{font-family:inherit;cursor:pointer;color:inherit;background:none;border:0}
input,textarea{font-family:inherit}
:focus-visible{outline:2px solid var(--cyan);outline-offset:2px;border-radius:6px}
.mono{font-family:"Space Mono",monospace}
.disp{font-family:"Monoton",cursive;font-weight:400}
.arc{font-family:"Audiowide",cursive}
.hidden{display:none!important}

/* ── topbar ── */
.top{display:flex;align-items:center;gap:10px;padding:9px 12px;padding-top:max(9px,env(safe-area-inset-top));
  background:linear-gradient(180deg,rgba(10,8,22,.96),rgba(10,8,22,.78));
  border-bottom:1px solid var(--line);backdrop-filter:blur(8px);position:relative;z-index:30}
.brand{display:flex;align-items:baseline;gap:7px;min-width:0}
.brand .logo{font-family:"Monoton",cursive;font-size:1.35rem;letter-spacing:1px;line-height:1;
  color:#fff;text-shadow:0 0 6px var(--cyan),0 0 16px var(--cyan),0 0 30px rgba(25,240,255,.5);animation:flick 6s infinite}
.brand .logo i{color:var(--mag);text-shadow:0 0 6px var(--mag),0 0 16px var(--mag)}
@keyframes flick{0%,93%,96%,100%{opacity:1}94%{opacity:.55}95%{opacity:.8}97%{opacity:.6}}
.top .spacer{flex:1}
.pill{display:flex;align-items:center;gap:6px;font:700 10px "Space Mono",monospace;letter-spacing:1px;
  text-transform:uppercase;padding:7px 10px;border:1px solid var(--line);border-radius:999px;color:var(--dim);white-space:nowrap}
.pill .dot{width:7px;height:7px;border-radius:50%;background:var(--dim2)}
.pill.live{color:var(--lime);border-color:rgba(182,255,58,.4)}
.pill.live .dot{background:var(--lime);box-shadow:0 0 8px var(--lime);animation:pulse 1.1s infinite}
.pill.wait{color:var(--yel);border-color:rgba(255,225,77,.4)}
.pill.wait .dot{background:var(--yel);box-shadow:0 0 8px var(--yel);animation:pulse 1.1s infinite}
.pill.host{color:var(--mag);border-color:rgba(255,43,214,.4)}
.pill.host .dot{background:var(--mag);box-shadow:0 0 8px var(--mag)}
.pill.guest{color:var(--cyan);border-color:rgba(25,240,255,.4)}
.pill.guest .dot{background:var(--cyan);box-shadow:0 0 8px var(--cyan)}
.iconbtn{width:42px;height:42px;display:grid;place-items:center;border:1px solid var(--line);border-radius:12px;
  font-size:1.1rem;transition:.15s}
.iconbtn:hover{border-color:var(--cyan);color:var(--cyan);box-shadow:var(--glow-c)}
.iconbtn:active{transform:scale(.92)}
.iconbtn.accent{border-color:rgba(255,43,214,.5);color:var(--mag)}
.iconbtn.accent:hover{box-shadow:var(--glow-m)}
@keyframes pulse{50%{opacity:.3}}

/* ── sala: room anclado a la ZONA VISIBLE (arregla teclado móvil) ── */
.room{position:fixed;inset:0;height:calc(var(--vh)*100);display:flex;flex-direction:column}
.body{flex:1;min-height:0;display:grid;grid-template-columns:1fr;grid-template-rows:1fr;overflow:hidden}

/* pin box (solo host) */
.pinbox{display:flex;align-items:center;gap:10px;background:repeating-linear-gradient(45deg,#120e26 0 10px,#16102e 10px 20px);
  border-bottom:1px solid var(--line);padding:8px 12px}
.pinbox .lbl{font:700 9px "Space Mono",monospace;letter-spacing:2px;color:var(--dim);text-transform:uppercase}
.pinbox .pin{font-family:"Monoton",cursive;font-size:1.5rem;letter-spacing:5px;color:#fff;
  text-shadow:0 0 8px var(--mag),0 0 18px rgba(255,43,214,.6)}
.pinbox .grow{flex:1}
.pinbox .btn{font-family:"Audiowide",cursive;font-size:10px;letter-spacing:1px;padding:9px 13px;border-radius:10px;
  border:1px solid var(--line);transition:.15s;white-space:nowrap}
.pinbox .btn:hover{border-color:var(--cyan);color:var(--cyan);box-shadow:var(--glow-c)}

/* stage */
.stagecol{min-height:0;display:flex;flex-direction:column;overflow:hidden;position:relative}
.stage{position:relative;background:#000;aspect-ratio:16/9;width:100%;
  border:2px solid var(--line);border-radius:var(--r);overflow:hidden;margin:10px 12px 0;
  box-shadow:0 0 0 1px #000, 0 0 40px -8px rgba(25,240,255,.25), 0 0 60px -10px rgba(255,43,214,.25), inset 0 0 60px rgba(0,0,0,.7);
  flex:0 0 auto}
.stage video{position:absolute;inset:0;width:100%;height:100%;border:0;background:#000;display:block;object-fit:contain}
.stage iframe{position:absolute;inset:0;width:100%;height:100%;border:0;background:#000;display:block}
.floaters{position:absolute;inset:0;pointer-events:none;overflow:hidden;z-index:5}
.floater{position:absolute;bottom:-10px;font-size:2rem;filter:drop-shadow(0 0 8px rgba(255,255,255,.5));
  animation:rise 2.6s ease-out forwards;will-change:transform,opacity}
@keyframes rise{0%{transform:translateY(0) scale(.6);opacity:0}12%{opacity:1}100%{transform:translateY(-115vh) scale(1.25) rotate(var(--rot,0deg));opacity:0}}
.stage .badge{position:absolute;top:9px;left:9px;z-index:8;display:flex;gap:7px}
.stage .badge .b{font:700 9px "Space Mono",monospace;letter-spacing:1px;padding:4px 8px;border-radius:6px;
  background:rgba(7,6,13,.7);border:1px solid var(--line);backdrop-filter:blur(4px)}
.stage .badge .b.rec{color:var(--red);border-color:rgba(255,77,109,.5)}
.stage .badge .b.rec i{display:inline-block;width:6px;height:6px;border-radius:50%;background:var(--red);box-shadow:0 0 7px var(--red);margin-right:5px;animation:pulse 1s infinite}
.stage .badge .b.sync{color:var(--lime);border-color:rgba(182,255,58,.4)}

/* overlay controles tipo YouTube */
.veil{position:absolute;inset:0;z-index:7;display:flex;flex-direction:column;justify-content:space-between;
  background:linear-gradient(180deg,rgba(0,0,0,.55) 0%,transparent 22%,transparent 70%,rgba(0,0,0,.7) 100%);
  opacity:0;transition:opacity .25s;pointer-events:none}
.stage.show .veil{opacity:1;pointer-events:auto}
.stage.paused .veil{opacity:1;pointer-events:auto}
.veil .vtop{display:flex;justify-content:flex-end;gap:8px;padding:10px}
.veil .vtop button{width:40px;height:40px;border-radius:50%;background:rgba(7,6,13,.5);backdrop-filter:blur(4px);
  font-size:1.05rem;transition:.15s;border:1px solid rgba(255,255,255,.08)}
.veil .vtop button:hover{background:rgba(25,240,255,.18);color:var(--cyan)}
.veil .vcenter{display:flex;align-items:center;justify-content:center;gap:26px;align-self:center}
.veil .vcenter .sk{width:52px;height:52px;border-radius:50%;background:rgba(7,6,13,.45);backdrop-filter:blur(4px);
  font-size:1.1rem;transition:.15s}
.veil .vcenter .sk:hover{background:rgba(255,255,255,.16)}
.veil .vcenter .play{width:72px;height:72px;border-radius:50%;font-size:1.7rem;color:#fff;
  background:linear-gradient(120deg,var(--mag),var(--vio));box-shadow:0 0 26px rgba(255,43,214,.55);transition:.15s}
.veil .vcenter .play:active{transform:scale(.9)}
.veil .vbot{padding:0 14px 12px}
.veil .scrub2{display:flex;align-items:center;gap:10px}
.veil .scrub2 .t{font:700 11px "Space Mono",monospace;color:#fff;min-width:40px;text-shadow:0 1px 3px #000}
.veil .scrub2 .t.r{text-align:right}
.track{flex:1;height:6px;background:rgba(255,255,255,.22);border-radius:999px;position:relative;cursor:pointer}
.track:hover{height:9px}
.track .fill{position:absolute;inset:0 auto 0 0;width:0;background:linear-gradient(90deg,var(--cyan),var(--mag));border-radius:999px}
.track .knob{position:absolute;top:50%;width:14px;height:14px;border-radius:50%;background:#fff;
  transform:translate(-50%,-50%) scale(0);box-shadow:0 0 10px var(--cyan);left:0;transition:transform .15s}
.track:hover .knob,.stage.show .knob{transform:translate(-50%,-50%) scale(1)}

.stage .extover{position:absolute;inset:0;z-index:4;display:grid;place-items:center;text-align:center;padding:22px;
  background:repeating-linear-gradient(45deg,#0a0814 0 14px,#100c20 14px 28px)}
.stage .extover>div{display:grid;gap:11px;justify-items:center;max-width:440px}
.stage .extover b{font-family:"Audiowide",cursive;font-size:clamp(.95rem,2.4vw,1.3rem);color:#fff;line-height:1.3}
.stage .extover .dom{font:700 12px "Space Mono",monospace;color:var(--mag)}
.stage .extover p{font-size:12px;color:var(--dim);line-height:1.5}
.stage .extover a{font-family:"Audiowide",cursive;font-size:11px;color:#06121a;background:var(--cyan);
  padding:11px 18px;border-radius:10px;text-decoration:none;box-shadow:var(--glow-c)}
.idle{position:absolute;inset:0;z-index:3;display:grid;place-items:center;text-align:center;gap:6px;
  background:radial-gradient(120% 120% at 50% 0%,#120e26,#050409)}
.idle .disp{font-size:clamp(1.4rem,5vw,2.4rem);color:#fff;text-shadow:0 0 10px var(--cyan),0 0 26px rgba(25,240,255,.5);animation:flick 5s infinite}
.idle p{font:400 11px "Space Mono",monospace;letter-spacing:1px;color:var(--dim);text-transform:uppercase;padding:0 18px}
.pipclose{position:absolute;top:8px;right:8px;width:30px;height:30px;border-radius:50%;
  background:var(--red);color:#fff;display:none;place-items:center;font-size:.85rem;z-index:9;box-shadow:0 0 10px var(--red)}

/* chat */
.chatcol{min-height:0;display:flex;flex-direction:column;background:var(--bg2);border-top:1px solid var(--line);overflow:hidden}
.chathead{display:flex;align-items:center;gap:8px;padding:9px 12px;border-bottom:1px solid var(--line);position:relative}
.chathead .arc{font-size:.8rem;letter-spacing:1px;color:var(--cyan);text-shadow:0 0 10px rgba(25,240,255,.4)}
.chathead .roster{display:flex;gap:6px;flex-wrap:nowrap;flex:1;min-width:0;overflow-x:auto;scrollbar-width:none}
.chathead .roster::-webkit-scrollbar{display:none}
.chip{display:flex;align-items:center;gap:5px;font:600 10px "Space Mono",monospace;padding:3px 8px;border-radius:999px;
  background:var(--panel);border:1px solid var(--line);white-space:nowrap;flex:0 0 auto}
.chip i{width:6px;height:6px;border-radius:50%}
.modes{display:flex;gap:3px;background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:3px;flex:0 0 auto}
.modes button{font-size:.9rem;width:32px;height:30px;border-radius:7px;color:var(--dim);transition:.15s}
.modes button.on{background:linear-gradient(120deg,var(--cyan),var(--vio));color:#06121a;box-shadow:0 0 10px rgba(25,240,255,.4)}
.msgs{flex:1;min-height:0;overflow-y:auto;padding:12px 14px;display:flex;flex-direction:column;gap:9px;scroll-behavior:smooth}
.msgs::-webkit-scrollbar{width:8px}.msgs::-webkit-scrollbar-thumb{background:var(--line);border-radius:8px}
.msg{display:flex;flex-direction:column;gap:2px;animation:pop .22s both}
.msg .meta{display:flex;align-items:baseline;gap:7px}
.msg .nm{font:700 12px "Space Grotesk",sans-serif}
.msg .ts{font:400 9px "Space Mono",monospace;color:var(--dim2)}
.msg .tx{font-size:14px;line-height:1.45;color:var(--ink);word-break:break-word}
.msg.me .nm::after{content:" (tú)";color:var(--dim2);font-weight:400}
.msg.sticker .tx{font-size:2.7rem;line-height:1.1;filter:drop-shadow(0 0 10px rgba(255,255,255,.25))}
.msg.sys{align-items:center;text-align:center}
.msg.sys .tx{font:600 10px "Space Mono",monospace;letter-spacing:1px;color:var(--dim2);text-transform:uppercase;
  background:var(--panel);border:1px dashed var(--line);padding:5px 12px;border-radius:999px}
.empty{margin:auto;text-align:center;color:var(--dim2);font:400 12px "Space Mono",monospace;letter-spacing:.5px;padding:20px}
@keyframes pop{from{opacity:0;transform:translateY(8px)}}

/* composer + picker GRANDE */
.composer{border-top:1px solid var(--line);background:var(--panel);padding:8px 10px;padding-bottom:max(8px,env(safe-area-inset-bottom));position:relative;z-index:20}
.reactrow{display:flex;gap:6px;margin-bottom:8px;overflow-x:auto;padding-bottom:2px;scrollbar-width:none}
.reactrow::-webkit-scrollbar{display:none}
.reactrow button{font-size:1.4rem;width:44px;height:42px;flex:0 0 auto;border-radius:12px;border:1px solid var(--line);
  background:var(--panel2);transition:.12s}
.reactrow button:hover{border-color:var(--mag);transform:translateY(-2px)}
.reactrow button:active{transform:scale(.85)}
.inrow{display:flex;gap:8px;align-items:flex-end}
.inrow .emoji{width:48px;height:48px;flex:0 0 auto;border-radius:12px;border:1px solid var(--line);font-size:1.3rem;transition:.15s}
.inrow .emoji.on{border-color:var(--yel);color:var(--yel);box-shadow:0 0 12px rgba(255,225,77,.35)}
.inrow .txt{flex:1;min-width:0;background:var(--bg);border:1px solid var(--line);border-radius:12px;color:var(--ink);
  padding:13px;font-size:16px;max-height:120px;resize:none;line-height:1.4}
.inrow .txt:focus{outline:0;border-color:var(--cyan);box-shadow:var(--glow-c)}
.inrow .send{width:48px;height:48px;flex:0 0 auto;border-radius:12px;font-size:1.25rem;color:#06121a;
  background:linear-gradient(120deg,var(--cyan),var(--lime));box-shadow:0 0 16px rgba(25,240,255,.4);transition:.15s}
.inrow .send:active{transform:scale(.9)}
.picker{display:none;margin-bottom:8px;background:var(--bg);border:1px solid var(--line);border-radius:14px;overflow:hidden}
.picker.on{display:block;animation:pop .2s both}
.picker .tabs{display:flex;gap:4px;padding:7px;border-bottom:1px solid var(--line);overflow-x:auto;scrollbar-width:none}
.picker .tabs::-webkit-scrollbar{display:none}
.picker .tabs button{font:700 10px "Space Mono",monospace;letter-spacing:1px;text-transform:uppercase;padding:9px 12px;
  border-radius:9px;color:var(--dim);white-space:nowrap;border:1px solid transparent}
.picker .tabs button.on{color:#06121a;background:var(--cyan)}
.picker .grid{display:grid;grid-template-columns:repeat(6,1fr);gap:3px;padding:9px;max-height:42vh;overflow-y:auto}
.picker .grid button{font-size:1.7rem;aspect-ratio:1;border-radius:10px;transition:.1s}
.picker .grid button:hover{background:var(--panel2)}
.picker .grid button:active{transform:scale(.82)}

/* modos móvil */
body.m-cine .chatcol{position:absolute;left:0;right:0;bottom:0;height:70vh;z-index:40;transform:translateY(calc(100% - 50px));
  transition:transform .3s cubic-bezier(.4,0,.2,1);border-top:2px solid var(--mag);border-radius:18px 18px 0 0;
  box-shadow:0 -10px 40px rgba(0,0,0,.6)}
body.m-cine .chatcol.open{transform:translateY(0)}
body.m-cine .chathead{cursor:pointer;padding-top:16px}
body.m-cine .chathead::before{content:"";position:absolute;left:50%;top:6px;transform:translateX(-50%);width:42px;height:4px;border-radius:999px;background:var(--line)}
body.m-chat .stagecol{position:absolute;right:10px;bottom:96px;width:48vw;max-width:240px;z-index:45;
  border-radius:12px;overflow:visible;filter:drop-shadow(0 8px 24px rgba(0,0,0,.7))}
body.m-chat .stage{margin:0;aspect-ratio:16/9;border-color:var(--mag);box-shadow:0 0 24px rgba(255,43,214,.4)}
body.m-chat .pipclose{display:grid}

/* desktop */
@media(min-width:900px){
  .body{grid-template-columns:minmax(0,1.7fr) minmax(330px,1fr)}
  .stagecol{border-right:1px solid var(--line)}
  .chatcol{border-top:0}
  .stage{margin:14px}
  body.m-cine .chatcol{position:static;transform:none;height:auto;box-shadow:none;border-radius:0;border-top:0}
  body.m-cine .chathead{cursor:default;padding-top:9px}
  body.m-cine .chathead::before{display:none}
  body.m-chat .stagecol{position:static;width:auto;filter:none}
  body.m-chat .pipclose{display:none}
  .picker .grid{grid-template-columns:repeat(10,1fr)}
}
/* móvil: video GRANDE llenando (modo cine por defecto) */
@media(max-width:899px){
  body.m-cine .stage{aspect-ratio:auto;flex:1;min-height:0;margin:0;border:0;border-radius:0;box-shadow:inset 0 0 60px rgba(0,0,0,.7)}
  body.m-cine .stagecol{background:#000}
}

/* ── lobby ── */
.lobby{position:fixed;inset:0;z-index:200;display:grid;place-items:center;padding:18px;overflow:auto;
  background:radial-gradient(120% 90% at 50% 0%,rgba(255,43,214,.12),transparent 60%),var(--bg)}
.lobby .card{width:100%;max-width:440px;background:var(--panel);border:1px solid var(--line);border-radius:20px;
  padding:26px 22px;box-shadow:0 0 60px -10px rgba(25,240,255,.25),0 30px 60px -20px #000;position:relative;overflow:hidden}
.lobby .card::before{content:"";position:absolute;inset:0;border-radius:20px;padding:1px;
  background:linear-gradient(130deg,var(--cyan),transparent 40%,transparent 60%,var(--mag));
  -webkit-mask:linear-gradient(#000 0 0) content-box,linear-gradient(#000 0 0);-webkit-mask-composite:xor;mask-composite:exclude;pointer-events:none}
.lobby .disp{text-align:center;font-size:2.6rem;color:#fff;line-height:1;
  text-shadow:0 0 8px var(--cyan),0 0 22px var(--cyan),0 0 44px rgba(25,240,255,.4);animation:flick 6s infinite}
.lobby .disp i{color:var(--mag);text-shadow:0 0 8px var(--mag),0 0 22px var(--mag)}
.lobby .lede{text-align:center;font:400 12px "Space Mono",monospace;letter-spacing:1px;color:var(--dim);
  text-transform:uppercase;margin:10px 0 22px}
.lobby label{display:block;font:700 9px "Space Mono",monospace;letter-spacing:2px;color:var(--dim);text-transform:uppercase;margin:0 0 6px 2px}
.lobby input{width:100%;background:var(--bg);border:1px solid var(--line);border-radius:11px;color:var(--ink);
  padding:13px 14px;font-size:16px;margin-bottom:16px;transition:.15s}
.lobby input:focus{outline:0;border-color:var(--cyan);box-shadow:var(--glow-c)}
.lobby input::placeholder{color:var(--dim2)}
.lobby .pin-in{font-family:"Monoton",cursive;letter-spacing:6px;font-size:1.4rem;text-align:center}
.lobby .btn{width:100%;justify-content:center;display:flex;padding:14px;font-size:12px;margin-bottom:10px;
  font-family:"Audiowide",cursive;letter-spacing:1px;border-radius:12px;border:1px solid var(--line);transition:.15s}
.lobby .btn.primary{background:linear-gradient(120deg,var(--mag),var(--vio));border-color:transparent;color:#fff;box-shadow:0 0 18px rgba(255,43,214,.4)}
.lobby .btn.primary:hover{filter:brightness(1.12)}
.lobby .btn.lime{background:linear-gradient(120deg,var(--lime),var(--cyan));border-color:transparent;color:#06121a}
.lobby .btn:active{transform:translateY(1px)}
.lobby .div{display:flex;align-items:center;gap:10px;color:var(--dim2);font:700 9px "Space Mono",monospace;letter-spacing:2px;margin:6px 0 14px}
.lobby .div::before,.lobby .div::after{content:"";flex:1;height:1px;background:var(--line)}
.lobby .hint{font:400 10.5px "Space Mono",monospace;color:var(--dim2);text-align:center;margin:-6px 0 14px;letter-spacing:.3px}
.lobby .err{color:var(--red);font:600 11px "Space Mono",monospace;text-align:center;min-height:14px;margin-top:4px}

/* ── modal videoteca ── */
.sheet{position:fixed;inset:0;z-index:300;display:none;place-items:end center;background:rgba(3,2,8,.6);backdrop-filter:blur(3px)}
.sheet.on{display:grid;animation:fade .2s both}
@keyframes fade{from{opacity:0}}
.sheet .panel{width:100%;max-width:560px;max-height:88vh;background:var(--panel);border:1px solid var(--line);
  border-radius:20px 20px 0 0;display:flex;flex-direction:column;overflow:hidden;animation:slideUp .3s cubic-bezier(.4,0,.2,1) both;
  box-shadow:0 -20px 60px rgba(0,0,0,.6)}
@keyframes slideUp{from{transform:translateY(40px);opacity:0}}
.sheet .phead{display:flex;align-items:center;gap:10px;padding:14px 16px;border-bottom:1px solid var(--line)}
.sheet .phead .arc{font-size:.95rem;letter-spacing:1px;color:var(--mag);text-shadow:0 0 10px rgba(255,43,214,.4)}
.sheet .phead .x{margin-left:auto;width:38px;height:38px;border-radius:10px;border:1px solid var(--line);font-size:1.1rem}
.sheet .phead .x:hover{border-color:var(--red);color:var(--red)}
.sheet .pbody{overflow-y:auto;padding:16px;display:flex;flex-direction:column;gap:16px}
.sheet .lbl2{font:700 9px "Space Mono",monospace;letter-spacing:2px;color:var(--dim);text-transform:uppercase;margin-bottom:7px}
.sheet .loadrow{display:flex;gap:8px}
.sheet .loadrow input{flex:1;min-width:0;background:var(--bg);border:1px solid var(--line);border-radius:11px;color:var(--ink);
  padding:13px;font:400 13px "Space Mono",monospace}
.sheet .loadrow input:focus{outline:0;border-color:var(--cyan);box-shadow:var(--glow-c)}
.sheet .loadrow .go{font-family:"Audiowide",cursive;font-size:11px;letter-spacing:1px;padding:0 18px;border-radius:11px;color:#fff;
  background:linear-gradient(120deg,var(--mag),var(--vio));white-space:nowrap}
.sheet .note{font:400 11.5px "Space Grotesk",sans-serif;color:var(--dim);line-height:1.5;
  background:var(--bg);border:1px dashed var(--line);border-radius:11px;padding:11px 13px}
.sheet .note b{color:var(--cyan)}
.sheet .sites{display:flex;flex-wrap:wrap;gap:8px}
.sheet .sites a{font:600 11px "Space Mono",monospace;letter-spacing:.5px;padding:9px 13px;border-radius:10px;
  border:1px solid var(--line);color:var(--ink);text-decoration:none;transition:.15s}
.sheet .sites a:hover{border-color:var(--mag);color:var(--mag);box-shadow:var(--glow-m)}
.sheet .hist{display:flex;flex-direction:column;gap:7px}
.sheet .hist .row{display:flex;align-items:center;gap:10px;background:var(--bg);border:1px solid var(--line);border-radius:10px;padding:9px 12px;cursor:pointer;transition:.15s}
.sheet .hist .row:hover{border-color:var(--cyan)}
.sheet .hist .row .ti{flex:1;min-width:0;font-size:12.5px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.sheet .hist .row .ho{font:400 10px "Space Mono",monospace;color:var(--dim2)}
.sheet .hist .none{font:400 11px "Space Mono",monospace;color:var(--dim2);padding:4px}
@media(min-width:900px){.sheet{place-items:center}.sheet .panel{border-radius:20px;max-height:80vh}}

/* toasts */
#toasts{position:fixed;left:12px;right:12px;bottom:96px;z-index:500;display:flex;flex-direction:column;gap:8px;align-items:center;pointer-events:none}
@media(min-width:900px){#toasts{left:auto;right:18px;bottom:18px;align-items:flex-end;max-width:320px}}
.toast{pointer-events:auto;display:flex;align-items:center;gap:10px;background:var(--panel2);border:1px solid var(--line);
  border-left:4px solid var(--cyan);color:var(--ink);padding:11px 14px;border-radius:10px;font-size:12.5px;
  box-shadow:0 8px 24px rgba(0,0,0,.5);animation:slidein .25s both;max-width:100%}
.toast.mag{border-left-color:var(--mag)}.toast.lime{border-left-color:var(--lime)}.toast.red{border-left-color:var(--red)}
.toast.out{opacity:0;transform:translateY(8px);transition:.3s}
@keyframes slidein{from{opacity:0;transform:translateY(12px)}}
.filewarn{position:fixed;top:0;left:0;right:0;z-index:5000;background:var(--yel);color:#1a1500;
  font:700 11px "Space Mono",monospace;text-align:center;padding:7px 12px;letter-spacing:.5px}
@media(prefers-reduced-motion:reduce){*{animation:none!important;transition:none!important}}
</style>
</head>
<body class="m-cine">
<div class="scanlines"></div>
<div id="fileWarn" class="filewarn hidden">⚠ MODO ARCHIVO · el P2P necesita http · en VS Code dale a "Go Live"</div>

<!-- LOBBY -->
<div class="lobby" id="lobby">
  <div class="card">
    <div class="disp">STREAM<i>DANI</i></div>
    <p class="lede">gay el q lo lea</p>
    <label for="nick">tu apodo en la sala</label>
    <input id="nick" type="text" maxlength="16" placeholder="ej. nuvsito" autocomplete="off" spellcheck="false">
    <button class="btn primary" id="createBtn">＋ CREAR SALA NUEVA</button>
    <div class="div">O ENTRAR CON UN PIN</div>
    <label for="joinCode">pin de la sala (1–4 números)</label>
    <input id="joinCode" class="pin-in" type="text" inputmode="numeric" maxlength="4" placeholder="· · · ·" autocomplete="off">
    <p class="hint">pide el número a quien creó la sala</p>
    <button class="btn lime" id="joinBtn">UNIRSE A LA SALA →</button>
    <div class="err" id="lobbyErr"></div>
  </div>
</div>

<!-- SALA -->
<div class="room hidden" id="room">
  <div class="top">
    <div class="brand"><span class="logo">STREAM<i>DANI</i></span></div>
    <span class="spacer"></span>
    <span class="pill" id="netPill"><span class="dot"></span><span id="netTxt">—</span></span>
    <span class="pill live"><span class="dot"></span><span id="viewCount">1</span></span>
    <button class="iconbtn accent" id="libBtn" title="Videoteca / buscar">🔍</button>
    <button class="iconbtn" id="fsBtn" title="Pantalla completa">⛶</button>
    <button class="iconbtn" id="leaveBtn" title="Salir">⏻</button>
  </div>

  <div class="pinbox hidden" id="pinbox">
    <span class="lbl">pin</span><span class="pin mono" id="roomPin">—</span>
    <span class="grow"></span>
    <button class="btn" id="copyBtn">COPIAR</button>
    <button class="btn" id="shareBtn">COMPARTIR</button>
  </div>

  <div class="body">
    <section class="stagecol">
      <div class="stage paused" id="stage">
        <div class="badge">
          <span class="b rec"><i></i>EN VIVO</span>
          <span class="b sync hidden" id="syncBadge">SYNC</span>
        </div>
        <div class="floaters" id="floaters"></div>
        <div class="idle" id="idle"><div class="disp">SIN SEÑAL</div><p id="idleTxt">abre la videoteca 🔍 y carga una peli</p></div>
        <button class="pipclose" id="pipClose" title="volver">✕</button>
        <div class="veil" id="veil">
          <div class="vtop">
            <button id="muteBtn" title="silenciar">🔊</button>
            <button id="fsBtn2" title="pantalla completa">⛶</button>
          </div>
          <div class="vcenter">
            <button class="sk" id="backBtn" title="-10s">⏪</button>
            <button class="play" id="playBtn">▶</button>
            <button class="sk" id="fwdBtn" title="+10s">⏩</button>
          </div>
          <div class="vbot">
            <div class="scrub2">
              <span class="t mono" id="curT">0:00</span>
              <div class="track" id="track"><div class="fill" id="fill"></div><div class="knob" id="knob"></div></div>
              <span class="t r mono" id="durT">0:00</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="chatcol" id="chatcol">
      <div class="chathead" id="chatHead">
        <span class="arc">CHAT</span>
        <div class="roster" id="roster"></div>
        <div class="modes" id="modes">
          <button data-m="cine" class="on" title="Cine">🎬</button>
          <button data-m="mix" title="Mixto">◑</button>
          <button data-m="chat" title="Chat + PiP">💬</button>
        </div>
      </div>
      <div class="msgs" id="msgs"><div class="empty">silencio en la sala…<br>manda el primer mensaje 🍿</div></div>
      <div class="composer">
        <div class="picker" id="picker">
          <div class="tabs" id="pickTabs"></div>
          <div class="grid" id="pickGrid"></div>
        </div>
        <div class="reactrow" id="reactRow"></div>
        <div class="inrow">
          <button class="emoji" id="emojiBtn" title="stickers">😀</button>
          <textarea class="txt" id="chatInput" rows="1" placeholder="opina de la peli…" maxlength="400"></textarea>
          <button class="send" id="sendBtn" title="enviar">➤</button>
        </div>
      </div>
    </section>
  </div>
</div>

<!-- VIDEOTECA -->
<div class="sheet" id="sheet">
  <div class="panel">
    <div class="phead"><span class="arc">🔍 VIDEOTECA</span><button class="x" id="sheetX">✕</button></div>
    <div class="pbody">
      <div>
        <div class="lbl2">cargar video (se ve aquí, sincronizado)</div>
        <div class="loadrow">
          <input id="urlInput" type="text" placeholder="pega link .mp4 / .webm / youtube…" spellcheck="false" autocomplete="off">
          <button class="go" id="loadBtn">CARGAR</button>
        </div>
      </div>
      <div class="note">Los <b>.mp4 / .webm</b> y <b>YouTube</b> se ven dentro y se sincronizan con tus amigos. Otros sitios (xvideos, erome, etc.) <b>bloquean verse dentro por su propia seguridad</b>: ábrelos con los botones de abajo, copia el link del video y pégalo arriba.</div>
      <div>
        <div class="lbl2">ir a buscar (se abre en otra pestaña)</div>
        <div class="sites" id="sites">
          <a href="https://www.youtube.com/" target="_blank" rel="noopener">YouTube</a>
          <a href="https://www.google.com/search?tbm=vid" target="_blank" rel="noopener">Google Videos</a>
          <a href="https://www.xvideos.com/" target="_blank" rel="noopener">ok</a>
          <a href="https://www.erome.com/" target="_blank" rel="noopener">lo q sea</a>
          <a href="https://vimeo.com/" target="_blank" rel="noopener">si eso</a>
        </div>
      </div>
      <div>
        <div class="lbl2">cargados antes</div>
        <div class="hist" id="hist"><div class="none">aún no has cargado nada</div></div>
      </div>
    </div>
  </div>
</div>

<div id="toasts"></div>

<script>
(()=>{
"use strict";
const $=s=>document.querySelector(s);
const esc=s=>String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const coarse=matchMedia('(pointer:coarse)').matches;
const isFile=location.protocol==='file:';
if(isFile) $('#fileWarn').classList.remove('hidden');

/* viewport visible (arregla teclado móvil) */
function setVH(){const h=window.visualViewport?window.visualViewport.height:window.innerHeight;
  document.documentElement.style.setProperty('--vh',h/100+'px')}
setVH(); window.addEventListener('resize',setVH);
if(window.visualViewport){window.visualViewport.addEventListener('resize',setVH);window.visualViewport.addEventListener('scroll',setVH)}

const COLORS=['#19f0ff','#ff2bd6','#b6ff3a','#8b5cff','#ffe14d','#ff4d6d','#43d9c0','#ff9d3a'];
const pickColor=s=>{let h=0;for(let i=0;i<s.length;i++)h=(h*31+s.charCodeAt(i))>>>0;return COLORS[h%COLORS.length]};

const REACTS=['❤️','🔥','','😱','','🍿','','😍'];
const STICK={
  'Cine':['🎬','🍿','🎞️','📽️','🎟️','🌟','️','','🎵','','🎯','','🎭','','🎨','','🕹️','📺'],
  'Caras':['😀','😂','🤣','😍','🥰','','🤩','','🤯','😭','😡','🤡','','🫣','','🤤','','🤠','😏','🙄','😬','🥺','😈','👻'],
  'Fuego':['🔥','','✨','💫','☄️','🌈','💯','🚀','️','❄️','🌊','☀️','⭐','🌙','💥','🎆','🎇','🪩'],
  'Manos':['👍','👎','👏','🙌','🤝','🙏','','🤟','','🫶','','💅','️','','👊','✊','','🫠']
};
const isOnlyEmoji=t=>{const s=t.trim();return s.length>0&&s.length<=8&&/^(\p{Extended_Pictographic}|\p{Emoji_Modifier}|\uFE0F|\u200D)+$/u.test(s)};

const S={role:null,me:null,pin:null,peer:null,hostConn:null,conns:new Map(),roster:new Map(),
  url:null,type:null,playing:false,time:0,dur:0,muted:false,prevVol:1,
  applyingRemote:false,ytReady:false,ytPending:null,lastBroadcast:0,gotFirst:false};
let myName=localStorage.getItem('nc_nick')||'';
let myColor=COLORS[0];
let hideTimer=null;

function toast(msg,kind){const t=document.createElement('div');t.className='toast'+(kind?' '+kind:'');
  t.textContent=msg; $('#toasts').appendChild(t);
  setTimeout(()=>{t.classList.add('out');setTimeout(()=>t.remove(),320)},2800)}

/* historial videoteca */
function getHist(){try{return JSON.parse(localStorage.getItem('nc_hist'))||[]}catch(e){return[]}}
function pushHist(url){let h=getHist();let title=url;try{title=new URL(url).hostname.replace(/^www\./,'')}catch(e){}
  h=h.filter(x=>x.url!==url);h.unshift({url,title,ts:Date.now()});h=h.slice(0,8);
  localStorage.setItem('nc_hist',JSON.stringify(h));renderHist()}
function renderHist(){const h=getHist(),box=$('#hist');
  if(!h.length){box.innerHTML='<div class="none">aún no has cargado nada</div>';return}
  box.innerHTML=h.map(x=>`<div class="row" data-u="${esc(x.url)}"><span class="ti">${esc(x.title)}</span><span class="ho mono">${esc(shortP(x.url))}</span></div>`).join('')}
function shortP(u){try{const p=new URL(u).pathname;return p.length>1?p.slice(0,18):''}catch(e){return''}}

/* reproductor */
let ytAPI=null,ytPlayer=null;
function loadYTAPI(){return new Promise(res=>{
  if(window.YT&&window.YT.Player)return res();
  if(ytAPI)return ytAPI.then(res);
  ytAPI=new Promise(r=>{const s=document.createElement('script');s.src='https://www.youtube.com/iframe_api';
    const w=window.onYouTubeIframeAPIReady;window.onYouTubeIframeAPIReady=()=>{w&&w();r()};document.head.appendChild(s)});
  ytAPI.then(res)})}
function detect(url){
  if(/\.(mp4|webm|ogv|ogg|mov)(\?.*)?$/i.test(url))return 'file';
  if(/(?:youtube\.com\/(?:watch\?(?:.*&)?v=|shorts\/|embed\/|live\/)|youtu\.be\/)([\w-]{6,20})/.test(url))return 'youtube';
  return 'external';
}
function ytId(url){const m=url.match(/(?:youtube\.com\/(?:watch\?(?:.*&)?v=|shorts\/|embed\/|live\/)|youtu\.be\/)([\w-]{6,20})/);return m?m[1]:null}

async function loadSource(url){
  S.url=url; S.type=detect(url); S.ytReady=false;
  const stage=$('#stage');
  stage.querySelectorAll('video,iframe,.extover').forEach(e=>e.remove());
  $('#idle').classList.add('hidden');
  if(S.type==='file'){
    const v=document.createElement('video');v.src=url;v.playsInline=true;v.preload='metadata';    v.volume=S.muted?0:S.prevVol;
    v.addEventListener('loadedmetadata',()=>{S.dur=v.duration||0;updScrub()});
    v.addEventListener('timeupdate',()=>{if(!S.applyingRemote){S.time=v.currentTime;updScrub();if(S.role==='host')maybeBroadcast()}});
    v.addEventListener('play',()=>{if(!S.applyingRemote){S.playing=true;updPlay();if(S.role==='host')broadcastState()}});
    v.addEventListener('pause',()=>{if(!S.applyingRemote){S.playing=false;updPlay();if(S.role==='host')broadcastState()}});
    v.addEventListener('ended',()=>{S.playing=false;updPlay();if(S.role==='host')broadcastState()});
    v.addEventListener('error',()=>toast('no se pudo cargar ese archivo ✗','red'));
    stage.insertBefore(v,$('#floaters').nextSibling);
    S.video=v;S.yt=null;
  } else if(S.type==='youtube'){
    const id=ytId(url);const wrap=document.createElement('div');wrap.id='ytwrap';wrap.style.cssText='position:absolute;inset:0';
    stage.insertBefore(wrap,$('#floaters').nextSibling);S.video=null;S.yt=null;
    try{await loadYTAPI();
      ytPlayer=new YT.Player('ytwrap',{videoId:id,width:'100%',height:'100%',
        playerVars:{autoplay:0,rel:0,modestbranding:1,playsinline:1,origin:location.origin||undefined},
        events:{onReady:()=>{S.ytReady=true;S.dur=ytPlayer.getDuration()||0;updScrub();if(S.ytPending){applyYT(S.ytPending);S.ytPending=null}},
          onStateChange:e=>{if(S.applyingRemote)return;S.playing=(e.data===YT.PlayerState.PLAYING);S.time=ytPlayer.getCurrentTime()||0;updPlay();updScrub();if(S.role==='host')broadcastState()}}});
      S.yt=ytPlayer;
    }catch(err){showExt(url,'YouTube no cargó')}
  } else { showExt(url,'Sitio externo'); }
  if(S.role==='guest')$('#syncBadge').classList.remove('hidden');
  pushHist(url);
}
function showExt(url,title){let host='—';try{host=new URL(url).hostname.replace(/^www\./,'')}catch(e){}
  const d=document.createElement('div');d.className='extover';
  d.innerHTML=`<div><b>${esc(title)}<br>SIN SYNC</b><span class="dom mono">${esc(host)}</span>
    <p>Esta web bloquea el reproductor embebido por su propia seguridad, así que no se sincroniza. Ábrela en otra pestaña mientras chatean, o pega el link directo al .mp4 / YouTube para verlo aquí.</p>
    <a href="${esc(url)}" target="_blank" rel="noopener">ABRIR EN OTRA PESTAÑA ↗</a></div>`;
  $('#stage').insertBefore(d,$('#floaters').nextSibling)}
function applyYT(p){if(!S.yt||!S.ytReady)return;S.applyingRemote=true;try{
  if(typeof p.time==='number')S.yt.seekTo(p.time,true);if(p.playing)S.yt.playVideo();else S.yt.pauseVideo();
}catch(e){}setTimeout(()=>S.applyingRemote=false,250)}

function pPlay(){if(S.type==='file'&&S.video)S.video.play().catch(()=>{});else if(S.yt&&S.ytReady)S.yt.playVideo()}
function pPause(){if(S.type==='file'&&S.video)S.video.pause();else if(S.yt&&S.ytReady)S.yt.pauseVideo()}
function pSeek(t){S.applyingRemote=true;if(S.type==='file'&&S.video)S.video.currentTime=t;else if(S.yt&&S.ytReady)S.yt.seekTo(t,true);S.time=t;updScrub();setTimeout(()=>S.applyingRemote=false,250)}
function pTime(){if(S.type==='file'&&S.video)return S.video.currentTime||0;if(S.yt&&S.ytReady)return S.yt.getCurrentTime()||0;return S.time}
function pPlaying(){if(S.type==='file'&&S.video)return !S.video.paused;if(S.yt&&S.ytReady)return ytPlayer.getPlayerState()===YT.PlayerState.PLAYING;return S.playing}
function pSetVol(v){if(S.video)S.video.volume=v}

function fmt(s){s=Math.max(0,s||0);const m=Math.floor(s/60),ss=Math.floor(s%60);return m+':'+String(ss).padStart(2,'0')}
function updScrub(){$('#curT').textContent=fmt(S.time);$('#durT').textContent=fmt(S.dur);
  const p=S.dur?Math.min(1,S.time/S.dur):0;$('#fill').style.width=(p*100)+'%';$('#knob').style.left=(p*100)+'%'}
function updPlay(){S.playing=pPlaying();$('#playBtn').textContent=S.playing?'❚❚':'▶';
  $('#stage').classList.toggle('paused',!S.playing);if(S.playing)scheduleHide();else showVeil()}

/* red P2P */
function send(conn,obj){try{if(conn&&conn.open)conn.send(obj)}catch(e){}}
function broadcast(obj,except){S.conns.forEach((c,id)=>{if(id!==except)send(c,obj)})}
function sendHost(obj){send(S.hostConn,obj)}

function handleData(fromId,obj){
  if(!obj||!obj.t)return;
  if(S.role==='host'){
    if(obj.t==='hello'){S.roster.set(fromId,{name:obj.name,color:obj.color});broadcast(obj,fromId);broadcastRoster();sendInitial(S.conns.get(fromId));sysMsg(obj.name+' entró a la sala');return}
    if(obj.t==='bye'){const r=S.roster.get(fromId);S.roster.delete(fromId);broadcast(obj,fromId);broadcastRoster();sysMsg((r&&r.name||'alguien')+' salió');return}
    if(obj.t==='cmd'){applyCmd(obj);broadcastState();return}
    if(obj.t==='load'){doLoad(obj.url,true);return}
    if(obj.t==='chat'){broadcast(obj);renderMsg(obj);return}
    if(obj.t==='react'){broadcast(obj);spawnFloater(obj.e);return}
  } else {
    if(!S.gotFirst){S.gotFirst=true;setNet('live','CONECTADO')}
    if(obj.t==='state'){applyState(obj);return}
    if(obj.t==='roster'){S.roster=new Map(Object.entries(obj.list));renderRoster();return}
    if(obj.t==='hello'){S.roster.set(fromId,{name:obj.name,color:obj.color});renderRoster();sysMsg(obj.name+' entró a la sala');return}
    if(obj.t==='bye'){S.roster.delete(fromId);renderRoster();sysMsg((obj.name||'alguien')+' salió');return}
    if(obj.t==='chat'){renderMsg(obj);return}
    if(obj.t==='react'){spawnFloater(obj.e);return}
  }
}
function sendInitial(conn){const list={};S.roster.forEach((v,k)=>list[k]=v);send(conn,{t:'roster',list});
  if(S.url)send(conn,{t:'state',url:S.url,type:S.type,playing:pPlaying(),time:pTime(),dur:S.dur||0})}
function applyCmd(o){
  if(o.c==='play')pPlay();else if(o.c==='pause')pPause();
  else if(o.c==='toggle'){pPlaying()?pPause():pPlay()}
  else if(o.c==='seek')pSeek(Math.max(0,o.time||0));
  else if(o.c==='delta')pSeek(Math.max(0,pTime()+(o.d||0)))}
function applyState(o){
  if(o.url&&o.url!==S.url){loadSource(o.url).then(()=>applyState(o));return}
  if(!S.url)return;
  S.dur=o.dur||S.dur;updScrub();S.applyingRemote=true;
  if(typeof o.time==='number'){if(Math.abs(pTime()-o.time)>0.7)pSeek(o.time);else S.time=pTime()}
  const want=!!o.playing;if(want!==pPlaying()){want?pPlay():pPause()}
  S.playing=want;updPlay();updScrub();setTimeout(()=>S.applyingRemote=false,300)}
function broadcastState(){const obj={t:'state',url:S.url,type:S.type,playing:pPlaying(),time:pTime(),dur:S.dur||0};
  S.playing=obj.playing;S.time=obj.time;updPlay();updScrub();broadcast(obj);S.lastBroadcast=Date.now()}
function maybeBroadcast(){if(Date.now()-S.lastBroadcast>2500)broadcastState()}
function broadcastRoster(){const list={};S.roster.forEach((v,k)=>list[k]=v);broadcast({t:'roster',list})}

function registerConn(conn){S.conns.set(conn.peer,conn);
  conn.on('data',d=>handleData(conn.peer,d));
  conn.on('close',()=>{S.conns.delete(conn.peer);const r=S.roster.get(conn.peer);
    if(r){S.roster.delete(conn.peer);if(S.role==='host')broadcastRoster();renderRoster();sysMsg((r.name||'alguien')+' se desconectó')}});
  conn.on('error',()=>{})}

const ICE={iceServers:[
  {urls:'stun:stun.l.google.com:19302'},{urls:'stun:stun1.l.google.com:19302'},
  {urls:'stun:stun.cloudflare.com:3478'},
  {urls:'turn:openrelay.metered.ca:80',username:'openrelayproject',credential:'openrelayproject'},
  {urls:'turn:openrelay.metered.ca:443',username:'openrelayproject',credential:'openrelayproject'},
  {urls:'turn:openrelay.metered.ca:443?transport=tcp',username:'openrelayproject',credential:'openrelayproject'}
]};
const PEER_PREF='nc26-';
const makeId=pin=>PEER_PREF+String(pin).padStart(4,'0');
const randPin=()=>Math.floor(1000+Math.random()*9000);

function startPeer(id){return new Promise((resolve,reject)=>{
  if(typeof Peer==='undefined'){reject('PeerJS no cargó (revisa tu conexión)');return}
  const peer=new Peer(id,{debug:1,config:ICE});
  const to=setTimeout(()=>{if(!S.peer){try{peer.destroy()}catch(e){}reject('tiempo de espera agotado (¿hay internet?)')}},12000);
  peer.on('open',pid=>{clearTimeout(to);S.peer=peer;resolve(pid)});
  peer.on('error',e=>{const msg=String(e&&e.type||e);clearTimeout(to);
    if(msg==='unavailable-id')reject('PIN_EN_USO');
    else if(msg==='peer-unavailable')reject('SALA_NO');
    else if(msg==='network'||msg==='server-error'||msg==='disconnected'){toast('red P2P caída · reconectando…','red');setTimeout(()=>{try{peer.reconnect()}catch(e){}},2500)}
    else if(!S.role)reject(msg);else toast('error P2P: '+msg,'red')});
  peer.on('disconnected',()=>{setTimeout(()=>{try{peer.reconnect()}catch(e){}},2000)})})}

function setNet(cls,txt){$('#netPill').className='pill '+cls;$('#netTxt').textContent=txt}

async function createRoom(){
  setErr('');if(!checkNick())return;
  let want=$('#createPin').value.trim().replace(/\D/g,'').slice(0,4);
  $('#createBtn').textContent='CREANDO…';$('#createBtn').disabled=true;
  const tries=want?[want]:[randPin(),randPin(),randPin(),randPin(),randPin()];
  let ok=false;
  for(const pin of tries){
    try{
      const id=await startPeer(makeId(pin));
      S.role='host';S.me=id;S.pin=pin;
      S.roster.set(id,{name:myName,color:myColor});
      S.peer.on('connection',c=>{c.on('open',()=>registerConn(c))});
      ok=true;break;
    }catch(e){if(e==='PIN_EN_USO'&&want){setErr('ese PIN ya está en uso, prueba otro');ok=false;break}
      else if(e==='PIN_EN_USO')continue;
      else{setErr('no se pudo crear la sala: '+e);break}}
  }
  if(ok){enterRoom();$('#roomPin').textContent=String(S.pin).padStart(4,'0');$('#pinbox').classList.remove('hidden');
    setNet('host','ANFITRIÓN');toast('sala creada · pin '+S.pin,'lime')}
  $('#createBtn').textContent='＋ CREAR SALA NUEVA';$('#createBtn').disabled=false;
}
async function joinRoom(){
  setErr('');if(!checkNick())return;
  const pin=$('#joinCode').value.trim().replace(/\D/g,'');
  if(!pin){setErr('pon el pin (1–4 números)');return}
  $('#joinBtn').textContent='CONECTANDO…';$('#joinBtn').disabled=true;setNet('wait','CONECTANDO');
  try{
    const id=await startPeer(makeId(randPin())); // id propio aleatorio
    S.role='guest';S.me=id;S.pin=pin;
    const conn=S.peer.connect(makeId(pin),{reliable:true,serialization:'json'});
    let opened=false;
    const to=setTimeout(()=>{if(!opened){setNet('wait','SIN RESPUESTA');setErr('nadie responde a ese pin · ¿está creada la sala?');$('#joinBtn').textContent='UNIRSE A LA SALA →';$('#joinBtn').disabled=false}},10000);
    conn.on('open',()=>{opened=true;clearTimeout(to);S.hostConn=conn;registerConn(conn);
      send(conn,{t:'hello',name:myName,color:myColor});enterRoom();setNet('wait','ESPERANDO SALA');toast('conectado ✓','cyan')});
    conn.on('error',()=>{clearTimeout(to);setErr('fallo al conectar con la sala');setNet('wait','ERROR')});
    conn.on('close',()=>{if(S.role==='guest'){toast('el anfitrión cerró la sala','red');setNet('wait','DESCONECTADO')}});
  }catch(e){if(e==='SALA_NO'){setErr('no existe una sala con ese pin');setNet('wait','NO EXISTE')}
    else setErr('no se pudo conectar: '+e);
    $('#joinBtn').textContent='UNIRSE A LA SALA →';$('#joinBtn').disabled=false}
}
function checkNick(){myName=$('#nick').value.trim().slice(0,16);
  if(!myName){setErr('pon un apodo primero');$('#nick').focus();return false}
  localStorage.setItem('nc_nick',myName);myColor=pickColor(myName);return true}
function setErr(m){$('#lobbyErr').textContent=m}

function enterRoom(){$('#lobby').classList.add('hidden');$('#room').classList.remove('hidden');renderRoster();
  $('#idleTxt').textContent=S.role==='host'?'abre la videoteca 🔍 y carga una peli':'esperando a que el anfitrión cargue la peli…';
  if(S.role==='host'){clearInterval(S._tick);S._tick=setInterval(()=>{if(S.url)broadcastState()},2500)}}

/* chat UI */
function renderRoster(){const r=$('#roster');r.innerHTML='';const all=new Map(S.roster);
  if(S.me&&!all.has(S.me))all.set(S.me,{name:myName,color:myColor});
  $('#viewCount').textContent=Math.max(1,all.size);
  all.forEach(v=>{const c=document.createElement('span');c.className='chip';c.innerHTML=`<i style="background:${v.color}"></i>${esc(v.name)}`;r.appendChild(c)})}
function renderMsg(o){const box=$('#msgs');const empty=box.querySelector('.empty');if(empty)empty.remove();
  const me=o.id===S.me;const div=document.createElement('div');const sticker=isOnlyEmoji(o.text);
  div.className='msg'+(me?' me':'')+(sticker?' sticker':'');
  div.innerHTML=`<div class="meta"><span class="nm" style="color:${o.color}">${esc(o.name)}</span><span class="ts mono">${fmtClock(o.ts)}</span></div><div class="tx">${esc(o.text)}</div>`;
  box.appendChild(div);if(box.scrollHeight-box.scrollTop-box.clientHeight<140)box.scrollTop=box.scrollHeight}
function sysMsg(t){const box=$('#msgs');const empty=box.querySelector('.empty');if(empty)empty.remove();
  const d=document.createElement('div');d.className='msg sys';d.innerHTML=`<div class="tx">${esc(t)}</div>`;box.appendChild(d);box.scrollTop=box.scrollHeight}
function fmtClock(ts){const d=new Date(ts);return String(d.getHours()).padStart(2,'0')+':'+String(d.getMinutes()).padStart(2,'0')}

function sendChat(){const inp=$('#chatInput');const text=inp.value.trim();if(!text)return;
  const obj={t:'chat',id:S.me,name:myName,color:myColor,text,ts:Date.now()};
  if(S.role==='host'){broadcast(obj);renderMsg(obj)}else sendHost(obj);
  inp.value='';inp.style.height='auto';
  if(coarse){inp.blur();closePicker()}else inp.focus()}
function sendReact(e){spawnFloater(e);const obj={t:'react',id:S.me,e};
  if(S.role==='host')broadcast(obj);else sendHost(obj)}
function spawnFloater(e){const f=document.createElement('span');f.className='floater';f.textContent=e;
  f.style.left=(8+Math.random()*82)+'%';f.style.setProperty('--rot',(Math.random()*40-20)+'deg');
  f.style.fontSize=(1.6+Math.random()*1.4)+'rem';$('#floaters').appendChild(f);setTimeout(()=>f.remove(),2700)}

function buildPicker(){const tabs=$('#pickTabs'),grid=$('#pickGrid');const cats=Object.keys(STICK);let cur=cats[0];
  tabs.innerHTML=cats.map((c,i)=>`<button class="${i?'':'on'}" data-c="${c}">${c}</button>`).join('');
  function render(c){grid.innerHTML=STICK[c].map(e=>`<button data-e="${e}">${e}</button>`).join('')}
  render(cur);
  tabs.onclick=ev=>{const b=ev.target.closest('button');if(!b)return;cur=b.dataset.c;
    tabs.querySelectorAll('button').forEach(x=>x.classList.toggle('on',x===b));render(cur)};
  grid.onclick=ev=>{const b=ev.target.closest('button');if(!b)return;$('#chatInput').value+=b.dataset.e;$('#chatInput').focus()}}
function buildReacts(){$('#reactRow').innerHTML=REACTS.map(e=>`<button data-e="${e}">${e}</button>`).join('')}
function closePicker(){$('#picker').classList.remove('on');$('#emojiBtn').classList.remove('on')}

function cmd(c,extra){if(S.role==='host'){applyCmd({c,...extra});broadcastState()}else sendHost({t:'cmd',c,...extra})}

/* scrub */
function scrubTo(clientX){const r=$('#track').getBoundingClientRect();const p=Math.min(1,Math.max(0,(clientX-r.left)/r.width));cmd('seek',{time:p*(S.dur||0)})}
let dragging=false;
$('#track').addEventListener('pointerdown',e=>{dragging=true;$('#track').setPointerCapture(e.pointerId);scrubTo(e.clientX);showVeil()});
$('#track').addEventListener('pointermove',e=>{if(dragging)scrubTo(e.clientX)});
$('#track').addEventListener('pointerup',()=>dragging=false);
$('#track').addEventListener('pointercancel',()=>dragging=false);

/* overlay auto-hide */
function showVeil(){$('#stage').classList.add('show');scheduleHide()}
function scheduleHide(){clearTimeout(hideTimer);hideTimer=setTimeout(()=>{if(pPlaying())$('#stage').classList.remove('show')},3000)}
$('#stage').addEventListener('pointerdown',e=>{if(e.target.closest('.veil button')||e.target.closest('.track'))return;
  if($('#stage').classList.contains('show')){$('#stage').classList.remove('show');clearTimeout(hideTimer)}else showVeil()});

/* mute / fs */
function toggleMute(){S.muted=!S.muted;pSetVol(S.muted?0:S.prevVol);$('#muteBtn').textContent=S.muted?'🔇':''}
function toggleFS(){const el=$('#stage');
  if(!document.fullscreenElement)(el.requestFullscreen||el.webkitRequestFullscreen||(()=>{})).call(el);
  else(document.exitFullscreen||document.webkitExitFullscreen||(()=>{})).call(document)}

/* modos */
function setMode(m){document.body.classList.remove('m-cine','m-mix','m-chat');document.body.classList.add('m-'+m);
  $('#modes').querySelectorAll('button').forEach(b=>b.classList.toggle('on',b.dataset.m===m));
  if(m!=='cine')$('#chatcol').classList.remove('open');setVH()}

/* videoteca */
function openSheet(){renderHist();$('#sheet').classList.add('on');setTimeout(()=>$('#urlInput').focus(),50)}
function closeSheet(){$('#sheet').classList.remove('on')}
function doLoad(url,fromNet){url=(url||'').trim();if(!url){toast('pega una URL primero','red');return}
  try{new URL(url)}catch(e){toast('esa URL no parece válida ✗','red');return}
  if(S.role==='host'){loadSource(url).then(()=>broadcastState());closeSheet()}
  else if(!fromNet){sendHost({t:'load',url});closeSheet();toast('pedido al anfitrión…','mag')}}

/* copiar / compartir pin */
async function copyPin(){try{await navigator.clipboard.writeText(String(S.pin));toast('pin copiado: '+S.pin,'cyan')}
  catch(e){toast('pin: '+S.pin,'mag')}}
async function sharePin(){const url=location.href.split('#')[0]+'#'+S.pin;
  const data={title:'NEONCAST',text:'entra a mi sala de cine 🎬 pin '+S.pin,url};
  if(navigator.share){try{await navigator.share(data)}catch(e){}}else{try{await navigator.clipboard.writeText(url);toast('link copiado ','cyan')}catch(e){copyPin()}}}

function leave(){try{if(S.role==='host')broadcast({t:'bye',name:myName});else sendHost({t:'bye',name:myName})}catch(e){}
  try{S.peer&&S.peer.destroy()}catch(e){}
  S.conns.clear();S.roster.clear();S.role=null;S.hostConn=null;S.url=null;S.gotFirst=false;clearInterval(S._tick);
  $('#room').classList.add('hidden');$('#lobby').classList.remove('hidden');
  $('#stage').querySelectorAll('video,iframe,.extover').forEach(e=>e.remove());
  $('#idle').classList.remove('hidden');$('#pinbox').classList.add('hidden');$('#syncBadge').classList.add('hidden');
  $('#msgs').innerHTML='<div class="empty">silencio en la sala…<br>manda el primer mensaje 🍿</div>'}

/* ── eventos ─ */
$('#createBtn').onclick=createRoom;
$('#joinBtn').onclick=joinRoom;
$('#nick').addEventListener('keydown',e=>{if(e.key==='Enter')$('#joinCode').focus()});
$('#joinCode').addEventListener('keydown',e=>{if(e.key==='Enter')joinRoom()});
$('#libBtn').onclick=openSheet;
$('#sheetX').onclick=closeSheet;
$('#sheet').addEventListener('pointerdown',e=>{if(e.target.id==='sheet')closeSheet()});
$('#loadBtn').onclick=()=>doLoad($('#urlInput').value);
$('#urlInput').addEventListener('keydown',e=>{if(e.key==='Enter')doLoad($('#urlInput').value)});
$('#hist').onclick=ev=>{const r=ev.target.closest('.row');if(r)doLoad(r.dataset.u)};
$('#playBtn').onclick=e=>{e.stopPropagation();cmd('toggle')};
$('#backBtn').onclick=e=>{e.stopPropagation();cmd('delta',{d:-10})};
$('#fwdBtn').onclick=e=>{e.stopPropagation();cmd('delta',{d:10})};
$('#muteBtn').onclick=e=>{e.stopPropagation();toggleMute()};
$('#fsBtn').onclick=toggleFS;$('#fsBtn2').onclick=e=>{e.stopPropagation();toggleFS()};
$('#leaveBtn').onclick=()=>{if(confirm('¿salir de la sala?'))leave()};
$('#copyBtn').onclick=copyPin;$('#shareBtn').onclick=sharePin;
$('#sendBtn').onclick=sendChat;
$('#chatInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat()}});
$('#chatInput').addEventListener('input',e=>{const t=e.target;t.style.height='auto';t.style.height=Math.min(120,t.scrollHeight)+'px'});
$('#chatInput').addEventListener('focus',()=>{if(document.body.classList.contains('m-cine'))$('#chatcol').classList.add('open');
  setTimeout(()=>{try{$('#chatInput').scrollIntoView({block:'nearest',behavior:'smooth'})}catch(e){}},320)});
$('#emojiBtn').onclick=()=>{const on=$('#picker').classList.toggle('on');$('#emojiBtn').classList.toggle('on',on);
  if(on&&coarse)$('#chatInput').blur()};
$('#reactRow').onclick=ev=>{const b=ev.target.closest('button');if(b)sendReact(b.dataset.e)};
$('#modes').onclick=ev=>{const b=ev.target.closest('button');if(b)setMode(b.dataset.m)};
$('#chatHead').onclick=ev=>{if(ev.target.closest('.modes'))return;if(document.body.classList.contains('m-cine'))$('#chatcol').classList.toggle('open')};
$('#pipClose').onclick=()=>setMode('mix');

/* deep link #pin */
(function(){const h=(location.hash||'').replace('#','').replace(/\D/g,'');if(h)$('#joinCode').value=h})();
if(myName)$('#nick').value=myName;

buildPicker();buildReacts();setMode(coarse?'cine':'mix');renderHist();

document.addEventListener('keydown',e=>{if(!$('#lobby').classList.contains('hidden'))return;
  if($('#sheet').classList.contains('on')){if(e.key==='Escape')closeSheet();return}
  const el=document.activeElement;if(/INPUT|TEXTAREA/.test(el.tagName))return;
  if(e.code==='Space'){e.preventDefault();cmd('toggle')}
  else if(e.key==='ArrowRight')cmd('delta',{d:5});
  else if(e.key==='ArrowLeft')cmd('delta',{d:-5});
  else if(e.key==='f'||e.key==='F')toggleFS();
  else if(e.key==='Escape'){$('#stage').classList.remove('show')}});
})();
</script>
</body>
</html>
